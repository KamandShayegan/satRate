# Cloudflare Pages: build output and KV binding for satRate
# See README for creating the KV namespace and deploying.

name = "satrate"
pages_build_output_dir = "dist"

# After creating a KV namespace in the dashboard (Workers & Pages > KV > Create namespace),
# add its id below and uncomment. Or add the binding in Pages project Settings > Functions > KV.
# [[kv_namespaces]]
# binding = "SATRATE_KV"
# id = "YOUR_KV_NAMESPACE_ID"
